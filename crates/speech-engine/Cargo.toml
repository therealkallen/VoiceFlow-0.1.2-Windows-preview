[package]
name = "speech-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
shared-protocol = { path = "../shared-protocol" }
ureq = { version = "2", features = ["json"] }
